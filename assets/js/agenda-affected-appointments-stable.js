/* Atendimentos afetados por ausência — ocorrência visível para todos; resolução avançada protegida por plano. */
(function(){
  'use strict';
  const STATUS_KEY='beautymove.mvp.professional.daily-status';
  const STATE_KEY='beautymove.mvp.state';
  const PEOPLE=['Ana','Bruna','Paula','Carla'];
  const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')||f;}catch(_){return f;}};
  const dateKey=()=>document.getElementById('agendaDatePicker')?.value||new Date().toISOString().slice(0,10);
  const mins=v=>{const [h,m]=String(v||'00:00').split(':').map(Number);return (Number(h)||0)*60+(Number(m)||0);};
  const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const statuses=()=>read(STATUS_KEY,{});
  const state=()=>read(STATE_KEY,{appointments:[],opportunities:[],transactions:[]});
  const canResolve=()=>window.BeautyMovePlan?.has?.('sos_opportunities')===true;
  function affected(){const d=dateKey(),data=statuses(),s=state();return (s.appointments||[]).filter(a=>a.date===d&&a.status!=='cancelado').map(a=>{const r=data[`${d}::${a.professional}`];if(!r||r.status!=='absent')return null;const ok=r.absenceType==='during_day'?mins(a.time)>=mins(r.absenceStart):(r.absenceType==='full_no_show'||r.absenceType==='full_notice');return ok?{...a,absence:r}:null;}).filter(Boolean);}
  function styles(){if(document.getElementById('affectedStableStyles'))return;const s=document.createElement('style');s.id='affectedStableStyles';s.textContent=`.agenda-impact-banner{margin:8px 0 0;padding:9px 12px;border:1px solid #efc2c2;border-radius:9px;background:#fff7f5;display:flex;align-items:center;justify-content:space-between;gap:12px;font-size:12px;color:#6f6878}.agenda-impact-banner[hidden]{display:none!important}.agenda-impact-banner strong{color:#b42318}.agenda-impact-banner button{border:0;border-radius:7px;background:#7139ef;color:#fff;padding:7px 11px;font-weight:700;cursor:pointer}.agenda-impact-banner button.is-locked{background:#fff;border:1px solid #d8ccef;color:#7139ef}.agenda-impact-banner .lock-note{font-size:10px;color:#6f6878;margin-left:4px}.affected-appointment-cell{box-shadow:inset 4px 0 #d92d20!important;background:#fff7f5!important}.affected-appointment-cell .affected-label{display:block;margin-top:5px;font-size:10px;font-weight:800;color:#b42318}.agenda-impact-modal{position:fixed;inset:0;z-index:10100;display:flex;align-items:center;justify-content:center;background:rgba(20,14,30,.35);padding:18px}.agenda-impact-card{width:min(620px,96vw);max-height:86vh;overflow:auto;background:#fff;border:1px solid #e5def4;border-radius:14px;box-shadow:0 18px 45px rgba(32,18,65,.22);padding:18px}.impact-item{border:1px solid #eee4ed;border-radius:10px;padding:11px;margin:8px 0}.impact-item strong,.impact-item small{display:block}.impact-item small{color:#6f6878;margin-top:2px}.impact-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.impact-actions button{border:1px solid #ddd6e6;background:#fff;border-radius:7px;padding:7px 9px;font:inherit;font-size:11px;font-weight:700;cursor:pointer}.impact-actions .primary{background:#7139ef;color:#fff;border-color:#7139ef}.impact-actions .danger{color:#b42318;border-color:#efc2c2}.impact-transfer{display:none;margin-top:8px}.impact-transfer select{width:100%;padding:8px;border:1px solid #ddd6e6;border-radius:7px}.impact-transfer button{margin-top:6px;width:100%;padding:8px;border:0;border-radius:7px;background:#7139ef;color:#fff;font-weight:700}`;document.head.appendChild(s);}
  function showUpgradeNotice(){const notice=document.getElementById('agendaNotice');if(!notice)return;notice.textContent='A Central de Oportunidades está disponível no plano pago. Seus atendimentos afetados continuam visíveis na Agenda.';notice.hidden=false;clearTimeout(showUpgradeNotice.timer);showUpgradeNotice.timer=setTimeout(()=>notice.hidden=true,5000);}
  function openModal(list){styles();document.getElementById('agendaImpactModal')?.remove();const m=document.createElement('div');m.id='agendaImpactModal';m.className='agenda-impact-modal';m.innerHTML=`<section class="agenda-impact-card"><div style="display:flex;justify-content:space-between"><div><h2 style="margin:0">Atendimentos afetados</h2><p style="font-size:12px;color:#6f6878">Há ${list.length} atendimento(s) que precisam de uma solução.</p></div><button id="impactClose" style="border:0;background:transparent;font-size:24px;cursor:pointer">×</button></div><div id="impactList"></div></section>`;document.body.appendChild(m);m.querySelector('#impactClose').onclick=()=>m.remove();m.onclick=e=>{if(e.target===m)m.remove();};const box=m.querySelector('#impactList');list.forEach(a=>{const item=document.createElement('div');item.className='impact-item';item.innerHTML=`<strong>${esc(a.time)} · ${esc(a.client||'Cliente')}</strong><small>${esc(a.professional)} · ${esc(a.service||'Atendimento')}</small><small>Profissional indisponível: ${esc(a.absence.absenceReason||'ausência registrada')}</small><div class="impact-actions"><button class="primary" data-action="transfer">Transferir</button><button data-action="sos">Acionar S.O.S.</button><button data-action="reschedule">Remarcar</button><button class="danger" data-action="cancel">Cancelar</button></div><div class="impact-transfer"><select><option value="">Selecionar profissional</option>${PEOPLE.filter(p=>p!==a.professional).map(p=>`<option>${p}</option>`).join('')}</select><button data-action="confirm">Confirmar transferência</button></div>`;box.appendChild(item);item.querySelector('[data-action="transfer"]').onclick=()=>item.querySelector('.impact-transfer').style.display='block';item.querySelector('[data-action="confirm"]').onclick=()=>transfer(a.id,item.querySelector('select').value,m);item.querySelector('[data-action="sos"]').onclick=()=>{m.remove();document.getElementById('requestSosButton')?.click();};item.querySelector('[data-action="reschedule"]').onclick=()=>{m.remove();document.querySelector(`[data-appointment-id="${CSS.escape(a.id)}"]`)?.click();};item.querySelector('[data-action="cancel"]').onclick=()=>cancel(a.id,m);});}
  function transfer(id,p,m){if(!p)return;const s=state(),a=s.appointments.find(x=>x.id===id);if(!a)return;a.professional=p;localStorage.setItem(STATE_KEY,JSON.stringify(s));m.remove();location.reload();}
  function cancel(id,m){const s=state(),a=s.appointments.find(x=>x.id===id);if(!a)return;a.status='cancelado';localStorage.setItem(STATE_KEY,JSON.stringify(s));m.remove();location.reload();}
  let last='';
  function render(){styles();const list=affected(),sig=JSON.stringify([dateKey(),list.map(a=>a.id),localStorage.getItem(STATUS_KEY),localStorage.getItem(STATE_KEY),canResolve()]);if(sig===last)return;last=sig;let banner=document.getElementById('agendaImpactBanner');if(!banner){banner=document.createElement('div');banner.id='agendaImpactBanner';banner.className='agenda-impact-banner';const shell=document.getElementById('agendaGrid');shell?.parentNode?.insertBefore(banner,shell);}if(list.length){banner.hidden=false;banner.innerHTML=`<span><strong>${list.length} atendimento(s) afetado(s)</strong> pela ausência.</span><button type="button" class="${canResolve()?'':'is-locked'}">Resolver oportunidade${canResolve()?'':' 🔒'}</button>${canResolve()?'':'<span class="lock-note">Plano pago</span>'}`;banner.querySelector('button').onclick=()=>canResolve()?openModal(affected()):showUpgradeNotice();}else{banner.hidden=true;banner.innerHTML='';}document.querySelectorAll('#agendaGrid [data-appointment-id]').forEach(cell=>{cell.classList.remove('affected-appointment-cell');cell.querySelector('.affected-label')?.remove();const a=list.find(x=>x.id===cell.dataset.appointmentId);if(a){cell.classList.add('affected-appointment-cell');const l=document.createElement('span');l.className='affected-label';l.textContent='Atendimento afetado · ação necessária';cell.appendChild(l);}});}
  function boot(){styles();render();const rerender=()=>{last='';setTimeout(render,120);};document.getElementById('agendaDatePicker')?.addEventListener('change',rerender);document.getElementById('prevDay')?.addEventListener('click',rerender);document.getElementById('nextDay')?.addEventListener('click',rerender);document.getElementById('todayBtn')?.addEventListener('click',rerender);document.getElementById('agendaInterval')?.addEventListener('change',rerender);window.addEventListener('storage',rerender);window.addEventListener('beautymove:sos-created',rerender);window.addEventListener('beautymove:sos-accepted',rerender);document.addEventListener('beautymove:planchange',rerender);let lastPoll='';setInterval(()=>{const sig=JSON.stringify([dateKey(),localStorage.getItem(STATUS_KEY),localStorage.getItem(STATE_KEY),canResolve()]);if(sig!==lastPoll){lastPoll=sig;last='';render();}},1200);}
  document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,350));
})();
