/* BeautyMove — Painel S.O.S. */
(function(){
  'use strict';
  const base='assets/';
  function ensureAssets(){
    if(!document.querySelector('link[data-sos-workspace-css]')){const l=document.createElement('link');l.rel='stylesheet';l.href=`${base}css/agenda-sos-workspace.css?v=20260824-8`;l.dataset.sosWorkspaceCss='1';document.head.appendChild(l)}
    if(document.querySelector('script[data-sos-workspace-js]'))return;
    const a=document.createElement('script');a.src=`${base}js/agenda-sos-workspace.js?v=20260824-7`;a.dataset.sosWorkspaceJs='1';a.onload=()=>{if(document.querySelector('script[data-sos-agenda-js]'))return;const b=document.createElement('script');b.src=`${base}js/agenda-sos.js?v=20260824-9`;b.dataset.sosAgendaJs='1';document.body.appendChild(b)};document.body.appendChild(a);
  }
  ensureAssets();
  const STATE='beautymove.mvp.state';
  const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}};
  const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const duration=v=>{const n=Number(v)||0,h=Math.floor(n/60),m=n%60;return h?`${h}h${m?` ${m}min`:''}`:`${m}min`};
  const statusLabel=v=>({aberta:'Aberta',em_busca:'Buscando profissionais',buscando:'Buscando profissionais',enviada:'Enviada ao profissional',aceita:'Aceita',accepted:'Aceita',em_andamento:'Em andamento',finalizada:'Finalizada',cancelada:'Cancelada'})[String(v||'').toLowerCase()]||String(v||'Aberta');
  function active(){const st=read(STATE,{opportunities:[]});return Array.isArray(st?.opportunities)?st.opportunities.filter(o=>!['finalizada','cancelada','concluida'].includes(String(o.status||'').toLowerCase())):[]}
  function setMainDate(date){const picker=document.getElementById('agendaDatePicker');if(!picker||!date)return;if(picker.value!==date){picker.value=date;picker.dispatchEvent(new Event('change',{bubbles:true}));window.dispatchEvent(new CustomEvent('beautymove:agenda-date-changed',{detail:{date}}))}}
  function focusOpportunity(id){const agenda=document.querySelector('.agenda-sos-agenda-card'),op=active().find(o=>String(o.id)===String(id));if(op?.date)setMainDate(op.date);if(!agenda){window.location.href=`sos.html?opportunity=${encodeURIComponent(id||'')}`;return}agenda.scrollIntoView({behavior:'smooth',block:'nearest',inline:'nearest'});setTimeout(()=>window.dispatchEvent(new CustomEvent('beautymove:sos-selected',{detail:{id:id||null}})),180)}
  function render(){const body=document.getElementById('agendaSosPanelBody'),count=document.getElementById('agendaSosCount');if(!body)return;const ops=active();if(count)count.textContent=`${ops.length} oportunidade${ops.length===1?'':'s'} ativa${ops.length===1?'':'s'}`;if(!ops.length){body.innerHTML='<div class="agenda-sos-empty">Nenhuma oportunidade S.O.S. ativa no momento.</div>';return}body.innerHTML=ops.map(o=>`<article class="agenda-sos-opportunity" data-sos-id="${esc(o.id)}" tabindex="0"><h3>${esc(o.service||'Serviço S.O.S.')}</h3><p><strong>Cliente:</strong> ${esc(o.client||'Cliente')}</p><p><strong>Data:</strong> ${esc(o.date||'')} · <strong>Horário:</strong> ${esc(o.time||'')}</p><p><strong>Especialidade:</strong> ${esc(o.specialty||'')} · <strong>Duração:</strong> ${duration(o.durationMinutes)}</p>${o.value!=null?`<p><strong>Valor:</strong> R$ ${Number(o.value||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</p>`:''}${o.radius?`<p><strong>Raio:</strong> ${esc(o.radius)}</p>`:''}${o.materials?`<p><strong>Material:</strong> ${esc(o.materials)}</p>`:''}${o.sentTo?`<p><strong>Profissional:</strong> ${esc(o.sentTo)}</p>`:''}${o.notes?`<p><strong>Obs.:</strong> ${esc(o.notes)}</p>`:''}<span class="agenda-sos-status">${esc(statusLabel(o.status))}</span><button type="button" class="agenda-sos-open-item" data-open-sos="${esc(o.id)}">Ver na Agenda S.O.S.</button></article>`).join('');body.querySelectorAll('[data-open-sos]').forEach(b=>b.addEventListener('click',e=>{e.stopPropagation();focusOpportunity(b.dataset.openSos)}));body.querySelectorAll('[data-sos-id]').forEach(card=>{card.addEventListener('click',()=>focusOpportunity(card.dataset.sosId));card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();focusOpportunity(card.dataset.sosId)}})})}
  function boot(){render();document.getElementById('agendaSosOpen')?.addEventListener('click',()=>focusOpportunity(null));window.addEventListener('storage',e=>{if(e.key===STATE)render()});window.addEventListener('beautymove:sos-changed',render)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
