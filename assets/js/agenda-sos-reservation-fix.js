/* BeautyMove — S.O.S. Agenda Final Stability Fix */
(function(){
  'use strict';
  const STATE_KEY='beautymove.mvp.state';
  const DURATIONS={'Corte':60,'Escova':30,'Coloração':120,'Luzes':180,'Corte feminino':60,'Corte masculino':45,'Manicure':60,'Pedicure':60,'Limpeza de pele':75,'Design de sobrancelhas':45};
  const read=()=>{try{return JSON.parse(localStorage.getItem(STATE_KEY)||'null')||{appointments:[],opportunities:[],transactions:[]};}catch(_){return{appointments:[],opportunities:[],transactions:[]};}};
  const write=s=>localStorage.setItem(STATE_KEY,JSON.stringify(s));
  const mins=t=>{const p=String(t||'00:00').split(':').map(Number);return (p[0]||0)*60+(p[1]||0);};
  const time=m=>`${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`;
  const esc=v=>String(v==null?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;');
  const label=m=>{m=Number(m)||0;const h=Math.floor(m/60),r=m%60;return h?(r?`${h}h ${r}min`:`${h}h`):`${r}min`;};
  const servicesOf=o=>{const list=Array.isArray(o?.servicesSnapshot)?o.servicesSnapshot:Array.isArray(o?.services)?o.services:[];if(list.length)return list.map(s=>({name:s?.name||'',duration:Number(s?.duration||s?.durationMinutes)||DURATIONS[String(s?.name||'').trim()]||30,value:Number(s?.clientPrice||s?.value||0)}));return String(o?.service||'').split('+').map(x=>x.trim()).filter(Boolean).map(name=>({name,duration:DURATIONS[name]||30,value:0}));};
  const durationOf=o=>{const total=servicesOf(o).reduce((n,s)=>n+Number(s.duration||0),0);if(total>0)return total;const direct=Number(o?.durationSnapshot||o?.durationMinutes||o?.duration||0);return direct>0?direct:30;};
  const endOf=(o,start=o?.time)=>time(mins(start)+durationOf(o));
  function injectVisualStandard(){if(document.getElementById('bmSosFinalVisual'))return;const style=document.createElement('style');style.id='bmSosFinalVisual';style.textContent=`#agendaGrid .bm-sos-client{font-size:15px!important;font-weight:900!important;line-height:1.15!important;display:block!important}#agendaGrid .bm-sos-service{font-size:11.5px!important;line-height:1.25!important;display:block!important;margin-top:2px!important}#agendaGrid .bm-sos-meta{font-size:10.5px!important;line-height:1.2!important;display:block!important;margin-top:2px!important}#agendaGrid .bm-sos-professional{font-size:12px!important;font-weight:900!important;line-height:1.2!important;display:block!important;margin-top:4px!important}#agendaGrid .bm-sos-continuation{font-size:10.5px!important;font-weight:700!important;line-height:1.2!important}#sosOpportunityPanel .sos-op-selected-candidate .sos-op-candidate-name{font-size:17px!important;font-weight:900!important;line-height:1.25!important}#sosOpportunityPanel .sos-op-selected-candidate .sos-op-candidate-data{font-size:12px!important;line-height:1.25!important}#detailsContent .detail-topline>div:nth-child(2)>strong{font-size:17px!important;font-weight:900!important}#detailsActions .bm-sos-reschedule{grid-column:1 / -1!important;background:#fff!important;border-color:#7438F5!important;color:#7438F5!important;font-weight:900!important}`;document.head.appendChild(style);}
  function currentDate(){return document.getElementById('agendaDatePicker')?.value||new Date().toISOString().slice(0,10);}
  function syncSosCells(){const grid=document.getElementById('agendaGrid');if(!grid)return;const state=read(),date=currentDate();const appointments=(Array.isArray(state.appointments)?state.appointments:[]).filter(a=>a&&a.date===date&&a.source==='sos'&&a.status!=='cancelado'&&(a.sosAcceptedBy||a.acceptedBy));const opportunities=(Array.isArray(state.opportunities)?state.opportunities:[]).filter(o=>o&&o.date===date&&o.source==='sos'&&o.status!=='cancelado'&&o.status!=='cancelada'&&o.status!=='resolved');const cells=[...grid.querySelectorAll('td[data-sos-cell="true"],td.sos-cell[data-sos-id],td.bm-sos-final-cell')];if(!cells.length)return;cells.forEach(cell=>{cell.className='sos-free-cell bm-sos-final-cell';cell.removeAttribute('data-sos-id');cell.removeAttribute('data-appointment-id');cell.innerHTML='Livre';});const paint=(item,isAppointment)=>{const start=mins(item.time||'08:00'),duration=durationOf(item),end=start+Math.max(30,duration),endTime=item.endTime||time(end);const professional=isAppointment?(item.sosAcceptedBy||item.acceptedBy||item.professional||'Profissional confirmada'):(item.acceptedBy||item.professional||'Aguardando profissional');cells.forEach(cell=>{const minute=mins(cell.dataset.time);if(minute<start||minute>=end)return;const isStart=minute===start;cell.className=`sos-cell sos-cell-found bm-sos-final-cell ${isStart?'sos-cell-start':'sos-cell-continuation'}`;if(isAppointment)cell.dataset.appointmentId=item.id;else cell.dataset.sosId=item.id;cell.dataset.sosCell='true';cell.innerHTML=isStart?`<strong class="bm-sos-client">${esc(item.client||'Cliente')}</strong><span class="bm-sos-service">${esc(item.service||'Atendimento')}</span><span class="bm-sos-meta">${esc(item.time||'')} – ${esc(endTime)} · ${label(duration)}</span><span class="bm-sos-professional">Profissional: ${esc(professional)}</span>`:`<span class="bm-sos-continuation">${esc(item.client||'Cliente')} · até ${esc(endTime)}</span>`;});};appointments.forEach(a=>paint(a,true));opportunities.forEach(o=>paint(o,false));}
  function ensureReservation(detail){if(!detail?.professional||!detail?.opportunity)return;const state=read();state.appointments=Array.isArray(state.appointments)?state.appointments:[];state.opportunities=Array.isArray(state.opportunities)?state.opportunities:[];const opportunity=state.opportunities.find(o=>o&&o.id===detail.opportunity.id)||detail.opportunity;const start=opportunity.time||'08:00',duration=durationOf(opportunity),professional=detail.professional;opportunity.durationSnapshot=duration;opportunity.endTime=endOf(opportunity,start);opportunity.acceptedBy=professional;opportunity.status='resolved';opportunity.acceptedAt=opportunity.acceptedAt||new Date().toISOString();if(opportunity.appointmentId){const existing=state.appointments.find(a=>a&&a.id===opportunity.appointmentId);if(existing){existing.source='sos';existing.sosAcceptedBy=professional;existing.sosAcceptedAt=opportunity.acceptedAt;existing.duration=duration;existing.durationMinutes=duration;existing.endTime=endOf(opportunity,existing.time||start);existing.services=servicesOf(opportunity);existing.service=opportunity.service||existing.services.map(s=>s.name).join(' + ');write(state);return;}}const id=`apt-sos-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,services=servicesOf(opportunity);state.appointments.push({id,date:opportunity.date,time:start,endTime:endOf(opportunity,start),professional,client:opportunity.client||'Cliente',services,service:opportunity.service||services.map(s=>s.name).join(' + '),duration,durationMinutes:duration,value:Number(opportunity.value||0)||services.reduce((n,s)=>n+Number(s.value||0),0),status:'agendado',source:'sos',sosAcceptedBy:professional,sosAcceptedAt:opportunity.acceptedAt,sosOpportunityId:opportunity.id});opportunity.appointmentId=id;write(state);}
  function addRescheduleButton(){const modal=document.getElementById('detailsModal');if(!modal||!modal.classList.contains('is-open'))return;const actions=document.getElementById('detailsActions');if(!actions)return;const sosId=actions.dataset.sosId||window.__bmCurrentSosId;if(!sosId||actions.querySelector('.bm-sos-reschedule'))return;const cancel=actions.querySelector('.action-cancel');if(!cancel)return;const button=document.createElement('button');button.type='button';button.className='action-button bm-sos-reschedule';button.textContent='Alterar horário';button.addEventListener('click',()=>{const state=read(),opportunity=state.opportunities.find(o=>o&&String(o.id)===String(sosId)),appointmentId=opportunity?.appointmentId||actions.dataset.appointmentId,appointment=state.appointments.find(a=>a&&String(a.id)===String(appointmentId));if(!appointment)return;modal.classList.remove('is-open');modal.setAttribute('aria-hidden','true');const appointmentModal=document.getElementById('appointmentModal');if(!appointmentModal)return;document.getElementById('appointmentId').value=appointment.id;document.getElementById('appointmentClient').value=appointment.client||'';document.getElementById('appointmentProfessional').value=appointment.professional||'';document.getElementById('appointmentTime').value=appointment.time||'';document.getElementById('appointmentTitle').textContent='Alterar horário';document.getElementById('appointmentMode').textContent='ALTERAR HORÁRIO';appointmentModal.dataset.bmSosRescheduleId=String(sosId);appointmentModal.classList.add('is-open');appointmentModal.setAttribute('aria-hidden','false');});actions.insertBefore(button,cancel);}
  function bindReschedule(){const form=document.getElementById('appointmentForm');if(!form||form.dataset.bmSosRescheduleBound==='1')return;form.dataset.bmSosRescheduleBound='1';form.addEventListener('submit',()=>{const modal=document.getElementById('appointmentModal'),sosId=modal?.dataset.bmSosRescheduleId;if(!sosId)return;setTimeout(()=>{const newTime=document.getElementById('appointmentTime')?.value;if(!newTime)return;const state=read(),opportunity=state.opportunities.find(o=>o&&String(o.id)===String(sosId)),appointmentId=document.getElementById('appointmentId')?.value,appointment=state.appointments.find(a=>a&&String(a.id)===String(appointmentId));if(opportunity&&appointment){opportunity.time=newTime;opportunity.endTime=endOf(appointment,newTime);opportunity.durationSnapshot=durationOf(appointment);opportunity.status='resolved';opportunity.acceptedBy=appointment.sosAcceptedBy||appointment.professional;opportunity.appointmentId=appointment.id;write(state);}delete modal.dataset.bmSosRescheduleId;syncSosCells();},180);},true);}
  function boot(){injectVisualStandard();bindReschedule();window.addEventListener('beautymove:sos-accepted',event=>{ensureReservation(event.detail||{});setTimeout(syncSosCells,0);setTimeout(syncSosCells,50);setTimeout(syncSosCells,150);setTimeout(syncSosCells,400);});window.addEventListener('beautymove:agenda-hydrated',()=>setTimeout(syncSosCells,50));const grid=document.getElementById('agendaGrid');if(grid){const observer=new MutationObserver(()=>{clearTimeout(window.__bmSosFinalTimer);window.__bmSosFinalTimer=setTimeout(syncSosCells,10);});observer.observe(grid,{childList:true,subtree:true});}let signature='';setInterval(()=>{const next=JSON.stringify([currentDate(),localStorage.getItem(STATE_KEY),document.getElementById('agendaGrid')?.innerHTML.length||0]);if(next!==signature){signature=next;syncSosCells();addRescheduleButton();}},250);setTimeout(syncSosCells,100);setTimeout(addRescheduleButton,300);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
