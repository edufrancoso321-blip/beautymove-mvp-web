/* BeautyMove — retired duplicate S.O.S. renderer.
   Visual rendering is owned exclusively by agenda-sos-actions-final.js.
   Kept as a compatibility file for older page references.
*/
(function(){
  'use strict';
  if(document.body?.dataset?.role!=='salao')return;
  window.__bmSosSingleAuthorityLoaded=false;
})();
