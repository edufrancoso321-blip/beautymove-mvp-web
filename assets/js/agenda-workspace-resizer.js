/* BeautyMove — resizer temporariamente congelado.
   IMPORTANTE: não altera a estrutura aprovada da Agenda.
   O redimensionamento será retomado somente depois da validação da base estável. */
(function(){
  'use strict';
  // Intencionalmente sem intervenção no DOM/layout.
})();
